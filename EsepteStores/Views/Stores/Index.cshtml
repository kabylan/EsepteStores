@model EsepteStores.Models.Store

@{
    ViewData["Title"] = "Index";
}

<br />
<a asp-action="Index" asp-controller="Home" class="p-1"><i class="fas fa-long-arrow-alt-left fa-lg btn btn-primary"></i></a>
<a asp-action="Edit" asp-route-id="@Model.Id" class="p-1"><i class="fas fa-edit fa-lg btn btn-primary"></i></a>
<br />
<br />

<h1>Мой магазин</h1>

@*<p>
        <a asp-action="Create">Create New</a>
    </p>*@

<div class="p-3">
    <div>
        <div class="row">
            <div class="p-2 col-6"><b>Название</b></div>
            <div class="p-2">@Model.Name</div>
        </div>
        <div class="row">
            <div class="p-2 col-6"><b>Пароль</b></div>
            <div class="p-2">@Model.Password</div>
        </div>
        <div class="row">
            <div class="p-2 col-6"><b>Логотип</b></div>
            <div>
                <img src="~/@Model.LogoFilePath" class="img-fluid rounded" style="width: 50px; height: 50px;" />
            </div>
        </div>
    </div>
    <br />
    @*<div class="row p-2">
            <a asp-action="Edit" asp-route-id="@Model.Id">Редактировать</a>
            <a asp-action="Index" asp-controller="Home" class="px-5">Назад</a>
        </div>*@
</div>